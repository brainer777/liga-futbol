{
  "name": "liga-futbol-root",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo de la Liga de Fútbol (backend + frontend)",
  "scripts": {
    "dev:init": "node scripts/dev-init.js",
    "backend:dev": "npm run start:dev --prefix backend",
    "frontend:dev": "npm run dev --prefix frontend",
    "db:reset": "npm run db:reset --prefix backend"
  }
}
